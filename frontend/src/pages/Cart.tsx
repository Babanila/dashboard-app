import type { FC } from "react";

const Cart: FC = () => {
	return (
		<div className="h-full flex flex-col justify-center items-center space-y-8 bg-primary">
			This is the page for cart where product in the basket are shown
		</div>
	);
};

export default Cart;
